import 'package:flutter/material.dart';

class FeedbackPage extends StatelessWidget {
  // const name({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("This is the Feedback Page");
  }
}
